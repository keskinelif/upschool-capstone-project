# Kopyala: cp .env.example .env  (Windows: copy .env.example .env)
# Gerçek değerleri .env dosyasına yaz — bu dosya Git'e gitmez.

GEMINI_API_KEY=
LLM_MODEL=gemini-2.5-flash
JWT_SECRET=change-me-in-production
